{
  "name": "@manuallab/database",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Database contracts and shared persistence utilities for ManualLab.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "pnpm --filter @manuallab/shared build && tsc -p tsconfig.json --noEmit",
    "test": "pnpm --filter @manuallab/shared build && pnpm --dir ../.. exec vitest run --config vitest.config.mts packages/database/src"
  },
  "dependencies": {
    "@manuallab/shared": "workspace:*"
  }
}
